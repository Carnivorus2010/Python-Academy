# Module 1 — Python Refresh

```python
project = "deepBlack"
version = 1
is_ready = True
components = ["dwl", "foot", "mako"]
tokens = {"accent_primary": "#7fa88a"}
```

Loop over a list:

```python
for component in components:
    print(component)
```

Loop over a dictionary:

```python
for name, value in tokens.items():
    print(f"{name}: {value}")
```

Open `exercises/module_01.py` and implement the missing functions.
