# Module 2 — Functions and Decisions

```python
def format_token(name: str, value: str) -> str:
    return f"{name}={value}"
```

A good function has one clear job, receives inputs as parameters, and returns a result.

Open `exercises/module_02.py` and complete the validators.
