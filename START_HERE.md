# Start Here

1. Read `lessons/01_python_refresh.md`.
2. Complete `exercises/module_01.py`.
3. Run:

```bash
pytest -q tests/test_module_01.py
```

4. When it passes, commit it:

```bash
git init
git add .
git commit -m "Complete Python Academy module 1"
```

Use this loop: predict, code, test, read the traceback, change one thing, test again.
