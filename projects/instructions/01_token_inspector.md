# Project 1 — Token Inspector

Create `token_inspector.py`.

Requirements:
- store at least six design tokens
- print them alphabetically
- count surface, text, and accent tokens
- reject malformed hex colors
- exit nonzero when invalid values are found

Stretch goals: `--category`, `--json`, and
verbose output.
