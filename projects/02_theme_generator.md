# Project 2 — Theme Generator

Read one JSON token file and generate:

- `generated/foot.ini`
- `generated/tokens.lua`
- `generated/theme.toml`

Validate colors, sort output consistently, support `--output-dir`, and add tests.
